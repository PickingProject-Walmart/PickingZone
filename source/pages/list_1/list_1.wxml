<!--pages/list_1/list_1.wxml-->

<view class="task_queue">
<button type='primary' class="refresh" bindtap="refreshPage">刷新</button>
  <switch wx:for="{{orderArray}}" data-index="{{index}}" wx:key="unique" bindtap="checkedReverse" class="orderList">
    <button class="bt">
      <text>{{item.id}}</text>
    </button>
  </switch>
</view>

<view class="bottom"></view>
